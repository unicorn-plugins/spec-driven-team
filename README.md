# Spec-Driven Team

> 느슨한 명세-코드 양방향 동기화로 소프트웨어를 유지보수하는 팀

---

## 개요

Spec-Driven Team은 명세와 코드 간 양방향 동기화를 통해 일관성을 보장하는 DMAP 플러그인입니다.
코드로부터 명세를 자동 생성하고, 명세 변경 시 코드를 자동 재생성하며, AI 어플리케이션 분리를 권고합니다.

**주요 기능:**
- 코드베이스 분석 + 명세화 가능 영역 분류
- 명세 자동 생성 (코드 → 명세) + 코드 자동 재생성 (명세 → 코드)
- 양방향 동기화 (느슨한 동기화) + 실시간 모니터링
- AI 어플리케이션 분리 권고 (MCP 서버, LangChain)
- 회귀 테스트 + 성능 비교

---

## 설치

### 사전 요구사항

- [Claude Code](https://claude.com/claude-code) CLI 설치
- Python 3.8+ (커스텀 도구 실행용)

### 플러그인 설치

**방법 1: 마켓플레이스 — GitHub (권장)**

```bash
# 1. GitHub 저장소를 마켓플레이스로 등록
claude plugin marketplace add unicorn-plugins/spec-driven-team

# 2. 플러그인 설치
claude plugin install spec-driven-team@unicorn-plugins

# 3. 설치 확인
claude plugin list
```

**방법 2: 마켓플레이스 — 로컬**

```bash
# 1. 로컬 경로를 마켓플레이스로 등록
claude plugin marketplace add ~/workspace/spec-driven-team

# 2. 플러그인 설치
claude plugin install spec-driven-team@local

# 3. 설치 확인
claude plugin list
```

> **설치 후 setup 스킬 실행:**
> ```
> /spec-driven-team:setup
> ```
> - `gateway/install.yaml`을 읽어 필수 MCP 서버 (context7) 자동 설치
> - 초기 디렉토리 생성 (specs/, .omc/, .backup/)
> - 상태 파일 초기화 (sync-pending.json, sync-history.json)

### 처음 GitHub을 사용하시나요?

다음 가이드를 참고하세요:

- [GitHub 계정 생성 가이드](https://github.com/unicorn-plugins/dmap/blob/main/resources/guides/github/github-account-setup.md)
- [Personal Access Token 생성 가이드](https://github.com/unicorn-plugins/dmap/blob/main/resources/guides/github/github-token-guide.md)

---

## 업그레이드

### Git Repository 마켓플레이스

```bash
# 마켓플레이스 업데이트 (최신 커밋 반영)
claude plugin marketplace update unicorn-plugins

# 플러그인 재설치
claude plugin install spec-driven-team@unicorn-plugins

# setup 재실행 (새 도구 설치)
/spec-driven-team:setup
```

### 로컬 마켓플레이스

```bash
# 1. 로컬 플러그인 소스 갱신
cd ~/workspace/spec-driven-team
git pull origin main

# 2. 마켓플레이스 업데이트
claude plugin marketplace update local

# 3. 플러그인 재설치
claude plugin install spec-driven-team@local

# 4. setup 재실행
/spec-driven-team:setup
```

---

## 사용법

### 슬래시 명령

| 명령 | 설명 |
|------|------|
| `/spec-driven-team:setup` | 초기 설정 (MCP 서버, 디렉토리 생성) |
| `/spec-driven-team:analyze` | 코드베이스 분석 + AI 앱 권고 |
| `/spec-driven-team:generate` | 명세 생성 (코드 → 명세) |
| `/spec-driven-team:sync` | 양방향 동기화 (명세 ↔ 코드) |
| `/spec-driven-team:watch` | 상태 모니터링 + 불일치 감지 |
| `/spec-driven-team:verify` | 회귀 테스트 + 성능 비교 |
| `/spec-driven-team:add-ext-skill` | 외부호출 스킬 추가 |
| `/spec-driven-team:remove-ext-skill` | 외부호출 스킬 제거 |
| `/spec-driven-team:help` | 사용 안내 |

### 워크플로우

#### Phase A: 탐색 (Explore)

**핵심 스킬:**
- **코드베이스 분석** (`analyze`) — 코드베이스 분석 및 명세화 영역 분류
- **명세 생성** (`generate`) — 코드 기반 명세 자동 생성

**실행:**
```
/spec-driven-team:analyze
→ 코드 분석 + 명세화 영역 분류 + AI 앱 권고

/spec-driven-team:generate
→ 명세 생성 (선언적 로직: 완전 명세, 복잡한 로직: 스켈레톤)
```

#### Phase B: 개발 (Develop)

**핵심 스킬:**
- **양방향 동기화** (`sync`) — 명세-코드 양방향 동기화
- **검증 및 테스트** (`verify`) — 회귀 테스트 및 성능 비교

**실행:**
```
# specs/ 디렉토리에서 명세 수정

/spec-driven-team:sync
→ 명세 → 코드 자동 재생성

/spec-driven-team:verify
→ 회귀 테스트 + 성능 비교
```

#### Phase C: 유지 (Maintain)

**핵심 스킬:**
- **상태 모니터링** (`watch`) — 동기화 상태 모니터링 및 불일치 감지

**실행:**
```
/spec-driven-team:watch
→ 상태 모니터링 (불일치 감지)

# 불일치 감지 시
/spec-driven-team:sync
→ 양방향 동기화
```

### 사용 예시

```
사용자: "프로젝트 코드를 분석해서 명세로 만들어줘"

1. /spec-driven-team:analyze
   → analyzer 에이전트가 전체 코드베이스 분석
   → 선언적 로직 28개, 복잡한 로직 7개 분류
   → AI 어플리케이션 분리 권고 3개 생성

2. /spec-driven-team:generate
   → spec-manager 에이전트가 명세 자동 생성
   → specs/ 디렉토리에 35개 명세 파일 생성
   → Git 커밋

결과: 코드 → 명세 완전 동기화 ✅
```

---

## 에이전트 구성

| 에이전트 | 티어 | 역할 |
|----------|------|------|
| analyzer | HIGH | 코드베이스 분석 + 명세화 영역 분류 + AI 앱 권고 |
| spec-manager | HIGH | 명세 생성 + 명세 현행화 + 명세 버전 관리 |
| code-generator | MEDIUM | 명세 → 코드 자동 재생성 (선언적 로직) |
| quality-guardian | MEDIUM | 동기화 모니터링 + 불일치 감지 + 회귀 테스트 |

---

## 요구사항

### 필수 도구

| 도구 | 유형 | 용도 |
|------|------|------|
| context7 | MCP | AI 프레임워크 공식 문서 검색 (LangChain, MCP) |
| spec_analyzer.py | Custom | AST 기반 명세화 가능 영역 분류 |
| sync_checker.py | Custom | 명세-코드 불일치 감지 |
| code_generator.py | Custom | 명세 → 코드 자동 생성 |

### 런타임 호환성

| 런타임 | 지원 |
|--------|:----:|
| Claude Code | ✅ |
| Codex CLI | 미검증 |
| Gemini CLI | 미검증 |

---

## 디렉토리 구조

### 플러그인 구조

```
spec-driven-team/
├── .claude-plugin/
│   ├── plugin.json
│   └── marketplace.json
├── skills/
│   ├── core/
│   ├── setup/
│   ├── help/
│   ├── add-ext-skill/
│   ├── remove-ext-skill/
│   ├── analyze/
│   ├── generate/
│   ├── sync/
│   ├── watch/
│   └── verify/
├── agents/
│   ├── analyzer/
│   ├── spec-manager/
│   ├── code-generator/
│   └── quality-guardian/
├── gateway/
│   ├── install.yaml
│   ├── runtime-mapping.yaml
│   ├── mcp/
│   │   └── context7.json
│   └── tools/
│       ├── spec_analyzer.py
│       ├── sync_checker.py
│       └── code_generator.py
├── commands/
│   ├── setup.md
│   ├── help.md
│   ├── add-ext-skill.md
│   ├── remove-ext-skill.md
│   ├── analyze.md
│   ├── generate.md
│   ├── sync.md
│   ├── watch.md
│   └── verify.md
└── README.md
```

### 사용자 프로젝트에 생성되는 출력 구조

```
your-project/
├── specs/                    # 명세 파일 (사용자 편집 가능)
│   ├── api/
│   ├── models/
│   └── services/
└── output/                  # 모든 스킬 실행 결과
    └── {project-name}/      # 프로젝트별 디렉토리
        ├── explore/         # Phase A: 탐색
        │   ├── analyze-result.md
        │   ├── codebase-analysis.md
        │   ├── ai-application-recommendations.md
        │   ├── generate-result.md
        │   └── specs/           # 생성된 명세 복사본
        ├── develop/         # Phase B: 개발
        │   ├── sync-result.md
        │   ├── sync-report.md
        │   ├── verify-result.md
        │   ├── quality-report.md
        │   ├── regression-test-results.json
        │   ├── performance-comparison.json
        │   └── backups/         # 코드 백업
        └── maintain/        # Phase C: 유지
            ├── watch-result.md
            ├── sync-pending.json
            ├── sync-history.json
            └── drift-detection-log.json
```

**프로젝트명 결정 규칙:**
1. 사용자 명시적 지정
2. Git 저장소명
3. package.json의 name
4. 현재 디렉토리명
5. 기본값: `project`

---

## 활용 케이스별 스킬 가이드

### 🔍 1. 프로젝트 처음 시작할 때

**목표:** 기존 코드베이스를 분석하고 명세 자동 생성

```
/spec-driven-team:analyze
→ 전체 코드 분석
→ 선언적 로직 vs 복잡한 로직 분류
→ AI 어플리케이션 분리 권고 제공

/spec-driven-team:generate
→ 코드 → 명세 자동 생성
→ specs/ 디렉토리에 명세 파일 저장
→ Git 커밋
```

**결과:** 코드와 명세가 완전히 동기화된 상태로 시작 ✅

---

### 📝 2. 명세 기반 개발 (Spec-Driven Development)

**목표:** 명세를 먼저 작성하고 코드 자동 생성

```
1. specs/ 디렉토리에 새 명세 작성
   예: specs/features/user-auth.yaml

2. /spec-driven-team:sync
   → 명세 → 코드 자동 재생성
   → 선언적 로직만 완전 재생성
   → 복잡한 로직은 스켈레톤만 생성

3. /spec-driven-team:verify
   → 회귀 테스트 실행
   → 성능 비교 (변경 전후)
```

**장점:**
- 설계를 명확히 하고 시작
- 보일러플레이트 코드 자동 생성
- 실수로 인한 불일치 방지

---

### 🔄 3. 코드 수정 후 명세 업데이트

**목표:** 코드 변경 사항을 명세에 반영 (역동기화)

```
# 코드 수정 후

/spec-driven-team:watch
→ 불일치 감지
→ specs/features/user-auth.yaml 변경 필요

/spec-driven-team:sync
→ 코드 → 명세 역동기화
→ 명세 파일 자동 업데이트
```

**사용 시나리오:**
- 긴급 버그 수정으로 코드 먼저 수정
- 프로토타이핑으로 코드 먼저 작성
- 외부 기여자가 명세 없이 코드 수정

---

### 🎯 4. AI 어플리케이션 분리하기

**목표:** MCP 서버, LangChain 등 AI 로직을 별도 모듈로 분리

```
/spec-driven-team:analyze
→ AI 어플리케이션 분리 권고 자동 생성
→ 예: "spec_analyzer를 MCP 서버로 분리 권장"

# 권고 사항 검토 후

/spec-driven-team:generate
→ 분리된 AI 모듈 명세 생성
→ MCP 서버 스켈레톤 생성
```

**효과:**
- AI 로직과 비즈니스 로직 분리
- 재사용 가능한 AI 컴포넌트
- 유지보수 용이

---

### 🧪 5. 리팩토링 시 안전성 보장

**목표:** 대규모 리팩토링 후 회귀 없음을 검증

```
# 리팩토링 전
/spec-driven-team:verify
→ 현재 성능 기록 (baseline)
→ 모든 테스트 통과 확인

# 리팩토링 수행

# 리팩토링 후
/spec-driven-team:verify
→ 회귀 테스트 실행
→ 성능 비교 (baseline vs 현재)
→ 차이 리포트 생성

/spec-driven-team:sync
→ 리팩토링 결과를 명세에 반영
```

**안전장치:**
- 자동 회귀 테스트
- 성능 저하 조기 발견
- 명세 자동 업데이트

---

### 🚨 6. 불일치 상시 모니터링

**목표:** 명세-코드 불일치를 실시간 감지

```
/spec-driven-team:watch
→ 백그라운드 모니터링 시작
→ 불일치 감지 시 알림
→ .omc/state/sync-pending.json 업데이트

# 알림 받으면
/spec-driven-team:sync
→ 불일치 해소 (양방향 동기화)
```

**사용 시나리오:**
- 다중 개발자 협업
- CI/CD 파이프라인 연동
- 장기 프로젝트 일관성 유지

---

### 🔧 7. 명세 버전 관리

**목표:** 명세 변경 이력 추적 및 롤백

```
/spec-driven-team:generate
→ 명세 생성 + Git 커밋
→ .backup/ 디렉토리에 백업

# 잘못된 변경 시
git revert HEAD
→ 명세 롤백

/spec-driven-team:sync
→ 롤백된 명세로 코드 재생성
```

**기능:**
- 명세 변경 이력 Git 관리
- 자동 백업 (.backup/)
- 버전별 비교 가능

---

### 🏗️ 8. 신규 기능 추가 워크플로우

**전체 프로세스:**

```
1. specs/features/ 에 새 명세 작성
   → YAML 형식으로 선언적 로직 명세
   → 복잡한 로직은 의사코드로만 작성

2. /spec-driven-team:sync
   → 명세 → 코드 생성
   → 선언적 로직 완전 생성
   → 복잡한 로직 스켈레톤 생성

3. 복잡한 로직 수동 구현
   → 스켈레톤에 구체적 로직 작성

4. /spec-driven-team:verify
   → 테스트 실행 + 성능 측정

5. /spec-driven-team:watch
   → 불일치 확인 (없어야 함)
```

---

### 🤝 9. 팀 협업 시나리오

**목표:** 여러 개발자가 명세 기반으로 협업

```
개발자 A:
1. specs/features/payment.yaml 수정
2. /spec-driven-team:sync
3. Git push

개발자 B:
1. Git pull
2. /spec-driven-team:watch
   → 불일치 감지 (로컬 코드 outdated)
3. /spec-driven-team:sync
   → 최신 명세로 코드 재생성
4. /spec-driven-team:verify
   → 충돌 없음 확인
```

**협업 규칙:**
- 명세를 single source of truth로 사용
- 코드 변경 후 반드시 sync 실행
- PR 전 verify로 회귀 검증

---

### ⚡ 10. CI/CD 파이프라인 연동

**목표:** 자동화된 명세-코드 동기화

```yaml
# .github/workflows/spec-sync.yml

name: Spec-Code Sync Check
on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check Sync
        run: |
          /spec-driven-team:watch
          if [ -s .omc/state/sync-pending.json ]; then
            echo "명세-코드 불일치 감지!"
            exit 1
          fi
      - name: Run Tests
        run: /spec-driven-team:verify
```

**자동화:**
- PR 시 불일치 자동 검출
- 회귀 테스트 자동 실행
- 성능 저하 자동 감지

---

## 라이선스

MIT License

---

## 문의 및 피드백

- GitHub: https://github.com/unicorn-plugins/spec-driven-team
- Issues: https://github.com/unicorn-plugins/spec-driven-team/issues
